# 📘 Assignment: Building REST APIs with FastAPI

## 🎯 Objetivo

Learn how to build RESTful APIs using the FastAPI framework. This assignment will guide you through creating endpoints, handling requests, and returning responses.

## 📝 Tarefas

### 🛠️ Task 1: Setup FastAPI Project

#### Description
Set up a new FastAPI project with the necessary dependencies and folder structure.

#### Requirements
Completed program should:

- Include a `main.py` file with a basic FastAPI app.
- Install FastAPI and Uvicorn as dependencies.
- Run the server locally using Uvicorn.

### 🛠️ Task 2: Create API Endpoints

#### Description
Create endpoints for a simple CRUD application (e.g., managing a list of tasks).

#### Requirements
Completed program should:

- Include endpoints for creating, reading, updating, and deleting tasks.
- Use appropriate HTTP methods (GET, POST, PUT, DELETE).
- Return JSON responses.

### 🛠️ Task 3: Add Validation and Error Handling

#### Description
Implement validation for incoming data and handle errors gracefully.

#### Requirements
Completed program should:

- Validate request data using Pydantic models.
- Return appropriate HTTP status codes for success and error cases.
- Include error messages in JSON format.